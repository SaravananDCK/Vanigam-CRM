@page "/ledgeraccount/{oid:guid}"
@using Vanigam.CRM.Client.Services
@using Vanigam.CRM.Objects.Entities
@using Vanigam.CRM.Client.Helpers
@inherits Vanigam.CRM.Client.Components.BaseDetailView<LedgerAccount, EditLedgerAccount>
@attribute [Authorize(Policy = Vanigam.CRM.Objects.ApplicationPolicy.IsAdministrator)]
@inject LedgerAccountApiService LedgerAccountApiService
@inject AccountGroupApiService AccountGroupApiService

<DetailPageTitleComponent TitleText="@Localizer[IsCreateMode ? "AddLedgerAccount" : "EditLedgerAccount"]" DialogService="@DialogService" CanEdit="@CanEdit" HasChanges="@HasChanges" CurrentOid="@Oid" />

@if (CurrentObject != null && IsReadOnlyMode && !IsCreateMode)
{
    <RadzenCard>
        @if (IsEditButtonVisible)
        {
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center"
                         JustifyContent="JustifyContent.End" Gap="10px">
                <RadzenFab Style="bottom: 12px; left: 12px;" Icon="edit" Click="@EnableEditMode" Size="ButtonSize.Medium" Title="@Localizer["Edit"]" />
            </RadzenStack>
        }
        <RadzenTabs @bind-SelectedIndex="ReadOnlyTabIndex" RenderMode="TabRenderMode.Client" TabPosition="TabPosition.Left">
            <Tabs>
                <RadzenTabsItem Text="@Localizer["BasicInformation"]" Icon="@FadIcon("fa-user")" class="fa-small">
                    <div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Code"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject?.Code ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Name"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject?.Name ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["AccountType"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject?.AccountType.ToString() ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["AccountGroup"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject?.AccountGroup?.Name ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["IsActive"]:</strong></RadzenText>
                            <RadzenBadge BadgeStyle="@BadgeHelper.GetBadgeStyleForBoolean(CurrentObject?.IsActive ?? false)" Text="@(CurrentObject?.IsActive == true ? Localizer["Active"] : Localizer["Inactive"])" class="rz-p-1" />
                        </div>

                        @if (!string.IsNullOrEmpty(CurrentObject.Description))
                        {
                            <div class="read-only-field rz-mb-3">
                                <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Description"]:</strong></RadzenText>
                                <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@CurrentObject.Description</RadzenText>
                            </div>
                        }
                    </div>
                </RadzenTabsItem>

                @* Address Information Tab *@
                <RadzenTabsItem Text="@Localizer["ContactInformation"]" Icon="@FadIcon("fa-home")" class="fa-small">
                    <div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Email"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.Email ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Phone"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.Phone ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Address"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.Address ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["City"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.City ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["State"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.State ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["PostalCode"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.PostalCode ?? "-")</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Country"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.Country ?? "-")</RadzenText>
                        </div>
                    </div>
                </RadzenTabsItem>

                @* Business Context Tab *@
                <RadzenTabsItem Text="@Localizer["FinancialInformation"]" Icon="@FadIcon("fa-briefcase")" class="fa-small">
                    <div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["Balance"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.Balance)</RadzenText>
                        </div>
                        <div class="read-only-field rz-mb-3">
                            <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.Span" class="field-label"><strong>@Localizer["CreditLimit"]:</strong></RadzenText>
                            <RadzenText TextStyle="TextStyle.Body1" TagName="TagName.Span" class="field-value">@(CurrentObject.CreditLimit)</RadzenText>
                        </div>
                    </div>
                </RadzenTabsItem>
            </Tabs>
        </RadzenTabs>
    </RadzenCard>
}
@if (CurrentObject != null && CanEdit && (!IsReadOnlyMode || IsCreateMode))
{
    <RadzenCard>
        <RadzenTemplateForm @ref=Form EditContext="EditContext" TItem="LedgerAccount" Data="@CurrentObject" Submit="@SaveAndStayInEdit">
            <FluentValidationValidator Validator="new Vanigam.CRM.Client.Validators.LedgerAccountValidator(Localizer)" />
            <ValidationSummary />

            <RadzenTabs @bind-SelectedIndex="EditTabIndex" RenderMode="TabRenderMode.Client" TabPosition="TabPosition.Left">
                <Tabs>
                    <RadzenTabsItem Text="@Localizer["BasicInformation"]" Icon="@FadIcon("fa-user")" class="fa-small">
                        <RadzenStack Gap="1rem">
                            <VanigamAccountingFormField Text=@Localizer["Code"]>
                                <ChildContent>
                                    <RadzenTextBox @bind-Value="@CurrentObject!.Code" Name="txt_Code" class="w-100" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["Name"]>
                                <ChildContent>
                                    <RadzenTextBox @bind-Value="@CurrentObject!.Name" Name="txt_Name" class="w-100" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["AccountType"]>
                                <ChildContent>
                                    @*<AccountTypeEnumDropDown @bind-Value="@CurrentObject!.AccountType" />*@
                                    <RadzenRadioButtonList @bind-Value=@CurrentObject.AccountType TValue="AccountType" JustifyContent="JustifyContent.Start" class="rz-m-2">
                                        <Items>
                                            @foreach (var type in AccountTypes)
                                            {
                                                <RadzenRadioButtonListItem Text="@Localizer[type.GetDisplayDescription().ToString()]" Value="type" />
                                            }
                                        </Items>
                                    </RadzenRadioButtonList>
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["AccountGroup"]>
                                <ChildContent>
                                    <AccountGroupDropDownDataGrid @bind-Value="@CurrentObject!.AccountGroupId" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["Department"]>
                                <ChildContent>
                                    <RadzenTextArea @bind-Value="@CurrentObject!.Description" Name="txt_Description" class="w-100" Rows="3" />
                                </ChildContent>
                            </VanigamAccountingFormField>
                            
                            <div class="rz-p-1 rz-text-align-left">
                                <RadzenCheckBox @bind-Value=@CurrentObject.IsActive Name="chk_IsActive" />
                                <RadzenLabel Text=@Localizer["Status"] Component="chk_IsActive" class="rz-ms-2" />
                            </div>
                        </RadzenStack>
                    </RadzenTabsItem>

                    <RadzenTabsItem Text="@Localizer["ContactInformation"]" Icon="@FadIcon("fa-phone")" class="fa-small">
                        <RadzenStack Gap="1rem">
                            <VanigamAccountingFormField Text=@Localizer["Email"]>
                                <ChildContent>
                                    <RadzenTextBox @bind-Value="@CurrentObject!.Email" Name="txt_Email" class="w-100" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["Phone"]>
                                <ChildContent>
                                    <VanigamAccountingTextBox @bind-Value="@CurrentObject.Phone" Name="txt_Phone" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["Address"]>
                                <ChildContent>
                                    <RadzenTextArea @bind-Value="@CurrentObject!.Address" Name="txt_Address" class="w-100" Rows="2" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["City"]>
                                <ChildContent>
                                    <RadzenTextBox @bind-Value="@CurrentObject!.City" Name="txt_City" class="w-100" />
                                </ChildContent>
                            </VanigamAccountingFormField>
                            
                            <VanigamAccountingFormField Text=@Localizer["State"]>
                                <ChildContent>
                                    <StatesDropDown @bind-Value="@CurrentObject.State" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["PostalCode"]>
                                <ChildContent>
                                    <VanigamAccountingTextBox @bind-Value="@CurrentObject.PostalCode" Name="txt_PostalCode" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["Country"]>
                                <ChildContent>
                                    <VanigamAccountingTextBox @bind-Value="@CurrentObject.Country" Name="txt_Country" />
                                </ChildContent>
                            </VanigamAccountingFormField>
                        </RadzenStack>
                    </RadzenTabsItem>

                    <RadzenTabsItem Text="@Localizer["FinancialInformation"]" Icon="@FadIcon("fa-map-marker-alt")" class="fa-small">
                        <RadzenStack Gap="1rem">
                            <VanigamAccountingFormField Text=@Localizer["Balance"]>
                                <ChildContent>
                                    <RadzenNumeric @bind-Value="@CurrentObject!.Balance" Name="num_Balance" class="w-100" TValue="decimal" Format="c" ReadOnly="true" />
                                </ChildContent>
                            </VanigamAccountingFormField>

                            <VanigamAccountingFormField Text=@Localizer["CreditLimit"]>
                                <ChildContent>
                                    <RadzenNumeric @bind-Value="@CurrentObject!.CreditLimit" Name="num_CreditLimit" class="w-100" TValue="decimal" Format="c" />
                                </ChildContent>
                            </VanigamAccountingFormField>
                        </RadzenStack>
                    </RadzenTabsItem>
                </Tabs>
            </RadzenTabs>

            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" JustifyContent="JustifyContent.End" Gap="0.5rem">
                @if (IsCreateMode)
                {
                    <VanigamAccountingSaveButton Id="btn_Save" Text="@Localizer["Save"]" Disabled="@(!(Form.EditContext.IsModified()))" @bind-IsBusy="IsBusy" />
                }
                else
                {
                    <VanigamAccountingSaveButton Id="btn_Update" Text="@Localizer["Update"]" Disabled="@(!(Form.EditContext.IsModified()))" @bind-IsBusy="IsBusy" />
                    <VanigamAccountingCancelButton Text="@Localizer["Cancel"]" Click="@EnableReadOnlyMode" />
                }
            </RadzenStack>
        </RadzenTemplateForm>
    </RadzenCard>
}
